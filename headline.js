const headline = {
    default: "Interface Oriented",
    alt: ""
}
const headline_array = [ headline.default ];

// AM i the right side of the window?
console.log(document.body.clientWidth);
console.log(document.body.clientHeight);